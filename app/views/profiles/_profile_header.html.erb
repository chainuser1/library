

<div class="panel">
  <div class="panel-heading" >
    <div class="navbar navbar-default">
      <div class="container-fluid">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#toggle-options" aria-expanded="false">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="#"><h5><%= current_user.profile.lname + " " +current_user.profile.fname %></h2><span class="badge glyphicon glyphicon-apple"></span></a>
        </div>
        <div class="collapse navbar-collapse " id="toggle-options">
          <ul class="nav navbar-nav">
            <li><%= link_to 'Edit', change_profile_path(CGI.escape_html(current_user.username)),method: :get%></li>
            <li><%= link_to 'Upload Photo', new_photo_path , class:"success button model-books",remote: true %></li>
            <li><%= link_to 'Delete Profile', delete_profile_path(CGI.escape_html(current_user.username)),remote: true,class: "warning button model-books" %></li>
            <li><%= link_to 'My Photos', photos_path , class:"text-warning " %></li>
          </ul>
        </div>
      </div>
    </div>
  </div>